import 'package:flutter/material.dart';

/// Squad Choice screen placeholder
class SquadChoiceScreen extends StatelessWidget {
  const SquadChoiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create or Join Squad')),
      body: const Center(
        child: Text('Squad Choice Screen - To be implemented'),
      ),
    );
  }
}
