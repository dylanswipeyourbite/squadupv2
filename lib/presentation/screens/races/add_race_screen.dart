import 'package:flutter/material.dart';

/// Add Race screen placeholder
class AddRaceScreen extends StatelessWidget {
  const AddRaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Race')),
      body: const Center(child: Text('Add Race Screen - To be implemented')),
    );
  }
}
